using System;

public class Test {
    public static void Main() {
        // We will write a small C# program using standard math to simulate Unity's Quaternion.Euler
        // Since we don't have UnityEngine.dll easily accessible in this sandbox without Unity,
        // we can just use a simple Python script instead, so I will do that in the next tool call if needed.
        Console.WriteLine("Ready.");
    }
}
